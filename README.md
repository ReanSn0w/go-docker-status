# Docker Status Page

Веб-приложение на Go, которое предоставляет красивую интерактивную страницу со статусом всех Docker контейнеров. Приложение подключается к Docker API, получает информацию о контейнерах и отображает её в удобном веб-интерфейсе.

## 🌟 Возможности

- ✅ Отображение списка всех Docker контейнеров в реальном времени
- 🔄 Показ статуса каждого контейнера (running, exited, created и т.д.)
- 🏷️ Поддержка custom labels для исключения контейнеров и переименования

## 🚀 Быстрый старт

### Локально

Убедитесь, что установлены:
- Go 1.25+
- Docker (должен быть запущен)

```bash
# Клонировать репозиторий
git clone https://github.com/ReanSn0w/go-docker-status.git
cd go-docker-status

# Собрать приложение
go build -o docker-status ./main.go

# Запустить приложение
./docker-status --port 8080
```

Приложение будет доступно по адресу: `http://localhost:8080`

### В Docker контейнере

```bash
# Собрать образ
docker build -t docker-status .

# Запустить контейнер
docker run -p 8080:8080 \
  -v /var/run/docker.sock:/var/run/docker.sock \
  docker-status
```

Приложение будет доступно по адресу: `http://localhost:8080`

### С Docker Compose

```yaml
version: '3.8'

services:
  docker-status:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - PORT=8080
```

```bash
docker-compose up
```

## ⚙️ Конфигурация

### Переменные окружения

| Переменная | Описание | Значение по умолчанию |
|-----------|---------|---------------------|
| `PORT` | Порт для запуска веб-сервера | `8080` |
| `DEBUG` | Включить режим отладки | `false` |

### Docker Labels

Приложение поддерживает специальные Docker labels для контроля отображения контейнеров:

#### Исключить контейнер из списка

Добавьте в конфиг контейнера:

```yaml
labels:
  - docker-status.exclude=true
```

#### Задать кастомное имя контейнера

```yaml
labels:
  - docker-status.name=MyCustomName
```

### Пример docker-compose.yml с labels

```yaml
version: '3.8'

services:
  my-app:
    image: my-app:latest
    labels:
      - docker-status.name=Custom App Name

  helper:
    image: helper:latest
    labels:
      - docker-status.exclude=true

  status-page:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
```

## 📋 Требования

- **Docker Socket**: Приложение требует доступа к `/var/run/docker.sock` для получения информации о контейнерах
- **Go 1.25+**: Для локальной разработки
- **Docker 20.10+**: Рекомендуемая версия Docker

## 🏗️ Архитектура

```
go-docker-status/
├── main.go                 # Точка входа приложения
├── internal/
│   ├── docker/            # Модуль для работы с Docker API
│   │   └── docker.go
│   └── server/            # Веб-сервер
│       └── server.go
├── tmpl/
│   └── status.html        # HTML шаблон страницы
├── Dockerfile             # Docker образ
├── go.mod                 # Go зависимости
└── README.md              # Этот файл
```

## 🔐 Безопасность

- Приложение читает Docker socket в режиме read-only
- Нет сохранения чувствительных данных
- HTML выход экранируется автоматически через template engine
- MIT лицензия

## 🛠️ Разработка

### Структура проекта

Проект использует:
- **chi**: Минималистичный HTTP router
- **docker/docker**: Официальный Docker client
- **go-pkgz/lgr**: Логирование

### Сборка Docker образа

```bash
docker build -t docker-status:latest .
```

Используется multi-stage build для минимального размера образа.

### Ошибка при запуске на Mac/Windows

Docker Desktop использует виртуальную машину. Убедитесь, что Docker Desktop запущен и работает корректно.

## 📝 Лицензия

MIT License - смотрите [LICENSE](LICENSE) файл для деталей
